logging:
  level:
    root: INFO

spring:
  liquibase:
    enabled: true
    change-log: classpath:db/changelog-root.yaml
  datasource:
    username: test
    password: test
    hikari:
      maximum-pool-size: 2
      minimum-idle: 1

app:
  giga-chat:
    auth-api-base-url: http://localhost:${mockServerPort:10035}
    api-base-url: http://localhost:${mockServerPort:10035}
    request-timeout-ms: 1000
    max-retries: 0
    retry-timeout-ms: 1000
    client-id: test
    auth-key: test
    auth-retry-timeout-ms: 100
    embeddings-inputs-max-count: 3
    stub-embeddings: false
    embeddings-model: EmbeddingsGigaR
  telegram:
    api:
      rest-api-base-url: http://localhost:${mockServerPort:10035}
      api-token: test
      update-limit: 100
      update-timeout-sec: 5
      request-timeout-ms: 10000
      max-retries: 0
    bot:
      enabled: false
      updates-queue-capacity: 100
      ai-model-type: GIGA_CHAT
      updates-handlers-count: 3
  llm-text-processor:
    base-url: http://localhost:${mockServerPort:10035}
    request-timeout-ms: 10000
    max-retries: 0
  ya-gpt:
    enabled: true
    retry-timeout-ms: 100000
    request-timeout-ms: 1000
    auth-api-base-url: http://localhost:${mockServerPort:10035}
    api-base-url: http://localhost:${mockServerPort:10035}
    o-auth-token: test
    max-retries: 1
    cloud-catalog-id: test
  hfs:
    api-base-url: http://localhost:${mockServerPort:10035}
    request-timeout-ms: 1000
    max-retries: 1
    retry-timeout-ms: 100
    base-folder: hfs-test
    user: giga-ai-agent
    password: abc12345
  kafka:
    bootstrap-servers: localhost:9092
    document-processing-topic: giga-ai-agent-documents-queue
    document-processing-error-topic: giga-ai-agent-documents-errors
    chunk-processing-topic: giga-ai-agent-recalculation-queue
    embeddings-recalculation-topic: giga-ai-agent-embeddings-recalculation-queue
    consumer-thread-pool-size: 1